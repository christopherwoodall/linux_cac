This is a fork of Jeremy Jackson's [linux-cac](https://github.com/jdjaxon/linux_cac) repo that installs the Linux CAC Modules using ansible.

To get started, run the following commands:
```
chmod +x setup.yml
./setup.yml
```
